<Window x:Class="DepotDownloaderGUI.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Steam Login" Height="280" Width="450"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Login to Steam"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource TextBrush}"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Username:"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,0,0,4"/>

        <TextBox Grid.Row="2"
                 x:Name="UsernameTextBox"
                 Margin="0,0,0,16"/>

        <TextBlock Grid.Row="3" Text="Password:"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,0,0,4"/>

        <PasswordBox Grid.Row="4"
                     x:Name="PasswordBox"
                     Margin="0,0,0,16"/>

        <CheckBox Grid.Row="5"
                  x:Name="RememberPasswordCheckBox"
                  Content="Remember password"
                  VerticalAlignment="Top"
                  Margin="0,0,0,16"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Login" Width="100" Margin="0,0,10,0"
                    Click="LoginButton_Click"
                    IsDefault="True"/>
            <Button Content="Cancel" Width="100"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
